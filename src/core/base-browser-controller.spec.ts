import { BaseBrowserController } from './base-browser-controller';

describe('BaseBrowserController', () => {
	it('should be defined', () => {
		// expect(new BaseBrowserController()).toBeDefined();
	});
});
