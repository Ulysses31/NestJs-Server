import { BaseEditorController } from './base-editor-controller';

describe('BaseEditorController', () => {
	it('should be defined', () => {
		// expect(new BaseEditorController()).toBeDefined();
	});
});
